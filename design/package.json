{
  "name": "driveme-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DriveMe — premium mobility landing site and lead-capture API (Mansio Group Oy)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node --check api/driver-location.js && node --check api/driver-trip.js && node --check api/driver-status.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
